{
  "homepage": "http://syednouman72.github.io/Job-Lister",
  "name": "web-assignment-1",
  "version": "0.0.1",
  "description": "",
  "keywords": [
    "assignment"
  ],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html images/* --out-dir dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "dev": "npm run build-img && parcel index.html --open",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-img": "parcel build images/* --no-cache"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "express": "^4.18.2",
    "gh-pages": "^6.0.0",
    "jquery": "^3.7.1"
  }
}
