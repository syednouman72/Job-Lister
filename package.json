{
  "name": "web-assignment-1",
  "version": "0.0.1",
  "description": "",
  "keywords": ["assignment"],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html && parcel build images/*",
    "dev": "bun run build-img && parcel index.html --open",
    "start": "bun server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-img": "parcel build images/* --no-cache"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "express": "^4.18.2",
    "jquery": "^3.7.1"
  }
}
